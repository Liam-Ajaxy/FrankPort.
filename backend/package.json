{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "generate-version": "node generate-version.js",
  "build": "npm run generate-version && echo 'Build complete'",
  "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "nodemailer": "^7.0.5"
  }
}
