// controllers/notificationController.js
export const getNotifications = (req, res) => {
  // Later: fetch from MySQL
  return res.json([
    { id: 1, message: "New message from contact form", read: false },
    { id: 2, message: "System update scheduled", read: true }
  ]);
};

export const markAsRead = (req, res) => {
  const { id } = req.params;
  // Later: update in MySQL
  return res.json({ message: `Notification ${id} marked as read` });
};
